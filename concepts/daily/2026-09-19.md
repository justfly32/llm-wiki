---
title: 2026-09-19 작업 일지
created: 2026-09-19
updated: 2026-09-19
type: daily
tags: [daily, worklog, cron-errors]
links: [[index]]
---

# 2026-09-19 작업 일지

## 오늘 변경 요약
- **MI8_project** — card-news md 51개 재발행(6/7채널 생성 + SEO 태그 보강), naver_blog 미리보기 업로드. 신규 .status 상태 정리 과정에서 Supabase 타임아웃 1건.
- **auto-trading** — `ma_rsi` 전략이 LiveEngine에 누락된 버그 수정(개별 종목 신호 미생성), 엔진 이중 구조(simulator+live 양쪽 수정 필요) 주의사항 문서화.
- **post1** — 개인 사이트 업데이트 1건.

## 주요 변경 파일
- [doc] `projects/MI8_project/Y_folder/card-news/md/*.md` — 카드뉴스 51개 재발행(상태/채널 갱신)
- [doc] `projects/MI8_project/Y_folder/card-news/.published_slugs.txt` — 발행 슬러그 목록 갱신

## git 커밋
- [post1] `fcfdde2` Update personal site 2026-09-18
- [auto-trading] `2d5d976` docs(strategy): 엔진 이중 구조 주의사항 — 전략 추가 시 simulator+live 양쪽 수정 필수
- [auto-trading] `863258c` fix(live): LiveEngine에 ma_rsi 전략 누락 수정 — 개별 종목 신호 미생성 버그
- [memories] `1a3bb69` chore: memory backup 2026-09-19 01:00

## 크론 에러 분석
- **1건** — `card-news-supabase-sync` (2026-09-18 23:56)
  - 에러 유형: `subprocess.TimeoutExpired` — `upload-card-news-md.py`의 상태 정리 단계 `curl -X PUT .../xxx.status.json`이 10초 타임아웃
  - 영향: 51개 카드뉴스 빌드·업로드·SEO 보강·naver_blog 업로드는 모두 **성공**했고, 마지막 `update_status()`(상태 완료 처리) 단계만 실패 → 데이터 손실 없음
- **공통 패턴 분석**: **3일 연속 재발 중** (09-15: 3건, 09-17: 2건, 09-18: 1건+오늘). 전부 `card-news-supabase-sync`의 Supabase Storage 상태 파일 curl 요청 10초 타임아웃. 지난 로그와 비교해 이번엔 DELETE가 아닌 **PUT(.status.json status=completed)** 에서 발생 — 같은 근본 원인(Supabase 대역폭/지연 + 10초 curl 타임아웃)으로 cleanup 경로 전반이 반복 실패.
- **⚠️ 조치 필요** — 동일 에러 유형이 3일 연속 반복(총 6+건 누적). 단순 재발이 아니라 근본 수정 필요.
- **개선 방향 제안**:
  1. `upload-card-news-md.py`의 모든 Supabase curl(`PUT`/`DELETE`) `--max-time` 10초 → 30~60초 상향
  2. 상태 파일 요청 실패 시 해당 slug는 건너뛰고 try/except + 재시도(1-2회) 로직 → 1건 실패가 잡 전체 실패로 이어지지 않게 분리
  3. (선택) 반복 타임아웃이 계속되면 cleanup을 대역폭 낮은 시간대로 이동 검토

## 메모 / 인사이트
- card-news-supabase-sync cleanup 타임아웃이 3일 연속(PUT/DELETE 모두) 재발 — 발행은 정상적으로 완성되고 정리 단계만 실패하므로 데이터 안전성은 확보됨. 하지만 매일 1~3건씩 반복되는 만큼 curl 타임아웃 상향과 실패 시 continue 처리가 우선 해결 과제.
- auto-trading에서 LiveEngine에 ma_rsi 전략이 누락된 채로 있었던 버그 수정 — 시뮬레이터와 라이브가 전략을 이중 보유하므로 변경 시 양쪽 패치 필수라는 운영 규칙이 확립됨.
- 인덱스: 신규 6 / 갱신 67, 총 17,051개 파일.
- 레지스트리: 34개 폴더 (git 13 / 로컬 21) — 신규 폴더 없음.